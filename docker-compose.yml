version: '3.8'

services:
  youtube-bot:
    build: .
    container_name: youtube-telegram-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./temp:/app/temp
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8080
    env_file:
      - .env